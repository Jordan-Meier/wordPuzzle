describe('findVowel', function() {
  it("will find vowels in an entered string and return true", function() {
    expect(findVowel("horse")).to.equal(true);
  });
});

// describe('replaceVowel', function () {
//   it("will replace all vowels with '-' characters", function () {
//     expect(replaceVowel("apple")).to.equal("-ppl-")
//   });
// });
